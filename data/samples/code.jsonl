{"id": "code-py-001", "url": "https://github.com/example/repo/blob/main/src/queue.py", "lang": "en", "text": "from collections import deque\nfrom typing import Generic, TypeVar\n\nT = TypeVar('T')\n\nclass BoundedQueue(Generic[T]):\n    def __init__(self, capacity: int):\n        if capacity <= 0:\n            raise ValueError('capacity must be positive')\n        self._buf: deque[T] = deque(maxlen=capacity)\n\n    def push(self, item: T) -> None:\n        self._buf.append(item)\n\n    def pop(self) -> T:\n        return self._buf.popleft()\n\n    def __len__(self) -> int:\n        return len(self._buf)\n", "tokens": 122}
{"id": "code-rs-001", "url": "https://github.com/example/repo/blob/main/src/lib.rs", "lang": "en", "text": "use std::sync::atomic::{AtomicUsize, Ordering};\n\npub struct Counter {\n    inner: AtomicUsize,\n}\n\nimpl Counter {\n    pub const fn new() -> Self {\n        Self { inner: AtomicUsize::new(0) }\n    }\n\n    pub fn inc(&self) -> usize {\n        self.inner.fetch_add(1, Ordering::Relaxed)\n    }\n\n    pub fn get(&self) -> usize {\n        self.inner.load(Ordering::Relaxed)\n    }\n}\n", "tokens": 95}
{"id": "code-sql-001", "url": "https://github.com/example/repo/blob/main/migrations/001.sql", "lang": "en", "text": "CREATE TABLE conversations (\n    id BIGSERIAL PRIMARY KEY,\n    user_id BIGINT NOT NULL REFERENCES users(id),\n    started_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    closed_at TIMESTAMPTZ,\n    model_version TEXT NOT NULL\n);\n\nCREATE INDEX conversations_user_started_idx ON conversations (user_id, started_at DESC);\n", "tokens": 76}
{"id": "code-py-002", "url": "https://github.com/example/repo/blob/main/scripts/sliding.py", "lang": "en", "text": "def sliding_window_max(arr, k):\n    from collections import deque\n    q = deque()\n    out = []\n    for i, x in enumerate(arr):\n        while q and arr[q[-1]] <= x:\n            q.pop()\n        q.append(i)\n        if q[0] <= i - k:\n            q.popleft()\n        if i >= k - 1:\n            out.append(arr[q[0]])\n    return out\n", "tokens": 93}
{"id": "code-py-003", "url": "https://github.com/example/repo/blob/main/notebooks/attn.py", "lang": "en", "text": "import torch\n\ndef scaled_dot_product(q, k, v, mask=None, dropout=0.0):\n    d_k = q.size(-1)\n    scores = torch.matmul(q, k.transpose(-2, -1)) / (d_k ** 0.5)\n    if mask is not None:\n        scores = scores.masked_fill(mask == 0, float('-inf'))\n    weights = torch.softmax(scores, dim=-1)\n    if dropout > 0:\n        weights = torch.nn.functional.dropout(weights, p=dropout)\n    return torch.matmul(weights, v), weights\n", "tokens": 127}
